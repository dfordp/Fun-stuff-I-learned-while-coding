 const onPressed = () => {
        if(window !== undefined) {
            window.navigator.clipboard.writeText(`${Element(s) to be copied});
            toast.success("Copied to clipboard");
        }
    };
